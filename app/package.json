{
  "name": "snapper",
  "description": "Node.js web service interface to puppeteer/chrome for generating PDFs and PNGs from HTML.",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "./node_modules/.bin/pm2 start app.js --no-daemon --watch",
    "_start": "./node_modules/.bin/nodemon ./node_modules/.bin/node-debug app.js"
  },
  "dependencies": {
    "express": "^3.4.8",
    "jade": "^1.5.0",
    "async": "^0.9.0",
    "bunyan": "~1.3.3",
    "pm2": "~0.12.7",
    "puppeteer": "1.7.0"
  },
  "devDependencies": {
    "nodemon": "^1.0.15",
    "node-inspector": "^0.7.4"
  }
}
